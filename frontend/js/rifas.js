import { mountHeader } from './header.js'; import { RafflesAPI } from './api.js'; import { BRL } from './state.js'; mountHeader(); const grid=document.getElementById('raffles'); async function load(){ try{ const list=await RafflesAPI.list(); grid.innerHTML=(list||[]).map(r=>`<article class="card"><img src="${r.imagem||r.image||'img/icons/placeholder.svg'}"><div class="body"><h3>${r.titulo||r.title}</h3><div class="price">R$ ${BRL(r.preco||r.price)}</div><a class="btn btn-primary" href="rifa.html?id=${r._id}">Comprar</a></div></article>`).join('')||'<p>Sem rifas por enquanto.</p>'; }catch(e){ grid.innerHTML='<p>Falha ao carregar rifas.</p>'; } } load();