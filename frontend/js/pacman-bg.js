const c=document.getElementById('pacman-bg'); if(c){const x=c.getContext('2d');let W,H,t=0;function size(){W=c.width=innerWidth;H=c.height;}size();addEventListener('resize',size);(function loop(){x.clearRect(0,0,W,H);x.fillStyle='#0b0620';x.fillRect(0,0,W,H);x.fillStyle='#00e0ff';for(let i=10;i<W;i+=28){x.beginPath();x.arc(i,H/2,2.5,0,Math.PI*2);x.fill();}const px=(t*3)%(W+60)-30;const m=Math.abs(Math.sin(t*.25))*.6+.2;x.fillStyle='#ffde59';x.beginPath();x.moveTo(px,H/2);x.arc(px,H/2,12,m,Math.PI*2-m,false);x.closePath();x.fill();t+=.5;requestAnimationFrame(loop);})()}